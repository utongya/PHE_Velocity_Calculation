/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: 'export',
  basePath: '/PHE_Velocity_Calculation',
  images: {
    unoptimized: true,
  },
}

module.exports = nextConfig
